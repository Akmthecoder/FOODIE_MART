<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akmthecoder.foodiemart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FOODIEMART">
        <activity
            android:name=".home.MyCartActivity"
            android:exported="false" />
        <activity android:name=".home.FoodItemActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".preScreen.LoginIntoFoodHouseActivity" />
        <activity android:name=".preScreen.ForgetPasswordActivity" />
        <activity android:name=".preScreen.SignInActivity" />
        <activity
            android:name=".preScreen.SignUpActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".home.HomeActivity"></activity>
        <activity android:name=".preScreen.SplashActivity" />
    </application>

</manifest>